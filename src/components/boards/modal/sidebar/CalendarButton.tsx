import React from 'react';
import Button from '../../../common/Button';

const CalendarButton = ({ size }: { size: {} }) => {
  return (
    <div>
      <Button size={size}>Calendar</Button>
    </div>
  );
};

export default CalendarButton;
