import React from 'react';
import Button from '../../../common/Button';

const ChecklistButton = ({ size }: { size: {} }) => {
  return (
    <div>
      <Button size={size}>Checklist</Button>
    </div>
  );
};

export default ChecklistButton;
